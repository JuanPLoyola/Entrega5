<!-- res/drawable/edit_text_border.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" />
    <stroke
        android:width="1dp"
        android:color="@color/gray" /> <!-- Cambia el color según tus necesidades -->
    <corners android:radius="4dp" /> <!-- Ajusta el radio para bordes redondeados -->
</shape>
